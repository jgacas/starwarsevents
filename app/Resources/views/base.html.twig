<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>
            {%- if block ('title') -%}
                {{ block('title') }} | Star Wars Events
            {%- else -%}
                Event's from the galaxy, far far away
            {%- endif -%}
        </title>
        {%- block stylesheets -%}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
